[project]
name = "jingwei"
version = "0.1.0"
description = "A reinforcement framework."
authors = [
    {name = "ZheyangXU", email = "jishengxzy@hotmail.com"},
]
dependencies = [
    "numpy>=1.26.4",
    "gymnasium>=0.29.1",
    "torch>=2.2.1",
    "dynaconf>=3.2.4",
]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
